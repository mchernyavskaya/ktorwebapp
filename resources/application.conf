ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
    database {
        host = 127.0.0.1
        port = 3306
        name = ktorwebapp
        user = root
        pass = root
    }
}
